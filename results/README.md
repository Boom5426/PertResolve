# Results

Results are generated by `scripts/run_all_splits.py` and saved as CSV files.

- `results_v4_10metrics.csv` — Full grid: 20 methods × 5 splits × 4 genes × 10 metrics.

Regenerate with:
```bash
python scripts/run_all_splits.py --data_dir data/ --output results/results_v4_10metrics.csv
```
