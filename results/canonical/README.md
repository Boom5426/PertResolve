# Canonical result tables

Small, committed tables that back the main-figure and Supplementary-table numbers, so
every headline value regenerates from a tracked file. The figure scripts read these via
`manuscript/figures/remote_data.py` (`REM = results/canonical`). Previously these lived
under `results/_remote/unified/` (gitignored); they are promoted here for reproducibility.

Convention note: PDS has two documented conventions. The **multi-seed** convention (these
tables) is what the main text reports (PDS 0.49-0.52, all CIs overlap 0.50). The
**single-draw** grid (`results/results_v4_10metrics.csv`, `results/bootstrap_CIs.json`,
and the ESM2/AlphaMissense grid `results/reviewer_controls/rep_grid_summary.csv`) gives
lower values (~0.46) and backs the Supplementary Table 7 single-draw column.

| File | Backs | Notes |
|------|-------|-------|
| `definitive_summary.csv` | Fig 2b PDS forest, Fig 4g external models, SI Table 10, abstract "0.49-0.52" | multi-seed per-method PDS + bootstrap CI + `crosses` (only PerturbNet crosses=False) |
| `unified_multiseed.csv` | Fig 2b (alt per-method summary) | method, PDS_mean, sd, lo, hi |
| `unified_results5.csv` | per-gene/method breakdowns; `best_model_pds.csv` source | per (method, gene, split, variant) PDS_cos + pearson_delta |
| `best_model_pds.csv` | Fig 5b/5c "best honest model", SI Table 11 "Best model PDS" | per-gene best in-house head (max per-gene mean PDS_cos over the 18 heads) |
| `oracle_ceiling.csv` | Fig 5b/5c oracle ceiling, SI Table 11 | native-depth per-gene PDS_oracle + 95% CI |
| `controlled_recovery.csv` | Fig 5f/5g benchmark-resolution transition | per-gene ceiling, P(recover order), P(winner) |
| `pairwise_resolvability.csv` | Fig 3g, SI Table 11 "Pairs resolvable" | per-gene frac_pairs_resolvable, median_nn_dist |
| `classifier_two_sample.csv` | Fig 3h classifier AUROC | per-gene detect/ident/perm AUROC |
| `metric_family_floor.csv` | SI Table 4 metric-family invariance | window ratio under 5 distance families |
| `perturbnet_subspace_test.json` | Fig 4g / SI Table 10 PerturbNet footnote | subspace-inflation control: full-space-at-chance predictors project to 0.52; random-in-subspace 0.516; perm P=0.13 |
| `fig3b_selfnull_dist.csv` | Fig 3b representative-variant windows | per-variant D_self / D_null distributions |
| `unified_summary5.csv` | (diagnostic only; NOT canonical) | an alternative multi-seed run that disagrees with `definitive_summary.csv`; kept for provenance, not cited |

`best_model_pds.csv` is regenerated by `remote_data.best_model()` (max over the 18 in-house
heads of the per-gene mean PDS_cos in `unified_results5.csv`); the CSV is the pinned copy.

`pairwise_resolvability.csv` and `classifier_two_sample.csv` are computed on the curated
benchmark variant set (KRAS n=92, via the `allele_perturb_bench.csv` BENCH filter), matching
every other analysis; the plotted Fig 3g/3h values are unchanged from the pre-restriction run
(KRAS was already at the floor), so this is a traceability-consistency fix only.
