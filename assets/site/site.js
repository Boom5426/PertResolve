const panels={detection:{number:'01 / Detection',title:'Can the perturbation be distinguished from reference?',copy:'PertResolve measures whether a perturbation response separates reproducibly from the reference population under disjoint cell sampling.',note:'Detection answers whether there is a measurable response. It does not imply that nearby perturbations can be identified.'},identification:{number:'02 / Identification',title:'Can the response be distinguished from its relevant competitors?',copy:'Identification evaluates separation within the biologically relevant candidate pool, such as variants of the same gene rather than unrelated perturbations.',note:'A benchmark can support broad perturbation detection while remaining unresolved for fine-grained allele identification.'},reproducibility:{number:'03 / Split-half reference',title:'Does the measured response reproduce across disjoint samples?',copy:'Independent cell splits provide an empirical reference for how consistently the experiment recovers the same perturbation-level structure.',note:'This reference characterizes the measurement. It is not a universal model-performance ceiling.'},ranking:{number:'04 / Model-ranking resolution',title:'Which predictor differences can the benchmark reliably order?',copy:'Controlled predictor differences are compared against measurement variability to ask which score gaps remain reproducibly rankable.',note:'Ranking resolution depends on the measured signal, candidate geometry, sampling depth and benchmark size.'}};document.querySelectorAll('.diagnostic').forEach(button=>{button.addEventListener('click',()=>{const panel=panels[button.dataset.panel];document.querySelectorAll('.diagnostic').forEach(item=>{item.classList.toggle('active',item===button);item.setAttribute('aria-selected',String(item===button))});document.querySelector('#panel-number').textContent=panel.number;document.querySelector('#panel-title').textContent=panel.title;document.querySelector('#panel-copy').textContent=panel.copy;document.querySelector('#panel-note').textContent=panel.note})});const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('#site-nav');menuButton?.addEventListener('click',()=>{const open=nav.classList.toggle('is-open');menuButton.setAttribute('aria-expanded',String(open))});nav?.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('is-open');menuButton?.setAttribute('aria-expanded','false')}));document.querySelectorAll('.copy-button').forEach(button=>button.addEventListener('click',async()=>{const target=document.querySelector(`#${button.dataset.copyTarget}`);if(!target)return;try{await navigator.clipboard.writeText(target.innerText);button.textContent='Copied';setTimeout(()=>button.textContent='Copy',1600)}catch{button.textContent='Select code'}}));